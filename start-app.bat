@echo off
REM This script opens the project's index.html file in the default web browser.

REM The "start" command opens a file with its default application.
REM The empty "" is a placeholder for the window title, which is a quirk of the start command needed for paths with spaces.
start "" "C:\.......\index.html"

exit 